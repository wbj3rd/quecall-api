import { Constructor } from '@loopback/core';
import { DefaultCrudRepository, Entity } from '@loopback/repository';
import { AbstractConstructor, IBaseEntity, ISoftCrudRepositoryMixin } from '../types';
export declare function SoftCrudRepositoryMixin<E extends Entity & IBaseEntity, ID, T extends Constructor<DefaultCrudRepository<E, ID, R>> | AbstractConstructor<DefaultCrudRepository<E, ID, R>>, R extends object = {}>(base: T): T & Constructor<ISoftCrudRepositoryMixin<E, ID, R>>;
